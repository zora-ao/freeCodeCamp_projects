# Prime Number Checker

A simple Node.js module that checks whether a given number is a prime number.

## Usage

The `isPrime` function returns `true` if the number is prime and `false` if it is not.

```js
const isPrime = require("./index.js");

console.log(isPrime(7)); // true
console.log(isPrime(10)); // false